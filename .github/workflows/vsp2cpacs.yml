name: VSP2CPACS

on:
  push:
    branches:
      - main
    paths:
      - "src/ceasiompy/vsp2cpacs/**"
      - "geometries/vspfiles/**"
      - "installation/ubuntu/openvsp.sh"
      - "environment.yml"
      - "setup.py"
      - "pyproject.toml"
      - ".github/workflows/vsp2cpacs.yml"
  pull_request:
    branches:
      - main
    paths:
      - "src/ceasiompy/vsp2cpacs/**"
      - "geometries/vspfiles/**"
      - "installation/ubuntu/openvsp.sh"
      - "environment.yml"
      - "setup.py"
      - "pyproject.toml"
      - ".github/workflows/vsp2cpacs.yml"
  workflow_dispatch:

jobs:
  vsp2cpacs-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup MiniConda3
      uses: conda-incubator/setup-miniconda@v3
      with:
        auto-update-conda: true
        python-version: "3.12"
        activate-environment: ceasiompy
        environment-file: environment.yml
        use-mamba: true

    - name: Install libGLU
      run: sudo apt-get install -y libglu1-mesa-dev

    - name: Install ceasiompy and OpenVSP
      shell: bash -l {0}
      run: |
        pip install -e .
        pip uninstall -y openvsp || true
        installation/ubuntu/openvsp.sh

    - name: Validate OpenVSP Python binding
      shell: bash -l {0}
      env:
        PYTHONPATH: ${{ github.workspace }}/installdir/OpenVSP/python:/usr/local/OpenVSP/python:${{ env.PYTHONPATH }}
      run: |
        python -c "import importlib; m = importlib.import_module('openvsp'); v = m if hasattr(m, 'XS_UNDEFINED') else importlib.import_module('openvsp.openvsp'); print(getattr(v, '__file__', None)); assert hasattr(v, 'XS_UNDEFINED'), 'OpenVSP binding missing XS_UNDEFINED'"

    - name: Run vsp2cpacs test
      shell: bash -l {0}
      env:
        PYTHONPATH: ${{ github.workspace }}/installdir/OpenVSP/python:/usr/local/OpenVSP/python:${{ env.PYTHONPATH }}
      run: |
        pytest -v src/ceasiompy/vsp2cpacs/tests/test_vsp2cpacs.py
